{
  "name": "founders-circle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "npm --prefix Frontend run dev",
    "dev:server": "npm --prefix Backend run dev",
    "build": "npm --prefix Frontend run build && npm --prefix Backend run build",
    "start": "npm --prefix Backend start",
    "lint": "npm --prefix Frontend run lint && npm --prefix Backend run lint",
    "typecheck": "npm --prefix Frontend run typecheck && npm --prefix Backend run typecheck",
    "test": "npm --prefix Backend test",
    "db:generate": "npm --prefix Backend run db:generate",
    "db:local": "npm --prefix Backend run db:local",
    "db:migrate": "npm --prefix Backend run db:migrate",
    "db:seed": "npm --prefix Backend run db:seed",
    "db:reset": "npm --prefix Backend run db:reset",
    "db:taxonomy": "npm --prefix Backend run db:taxonomy",
    "db:seed:community": "npm --prefix Backend run db:seed:community",
    "admin:create": "npm --prefix Backend run admin:create",
    "college:add": "npm --prefix Backend run college:add",
    "test:browser": "npm --prefix Backend run test:browser",
    "test:community:browser": "npm --prefix Backend run test:community:browser",
    "test:moderation:browser": "npm --prefix Backend run test:moderation:browser"
  }
}
