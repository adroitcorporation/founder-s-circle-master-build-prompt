# Browser-visible configuration only; never put secrets here.
# API calls use /api; Vite proxies to http://localhost:3001.
# Vercel proxies to Render using Frontend/vercel.json.
# VITE_API_URL is not used; browser requests stay on the frontend origin.
